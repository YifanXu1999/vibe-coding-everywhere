## Instruction: Directory Access Restrictions

**CRITICAL: You are strictly confined to the current workspace directory.**

### Allowed

- Only access files and directories within: `/Users/yifanxu/machine_learning/mobile/workspace/`
- All file operations (read, write, edit, glob, grep) must be within this directory
- Use paths relative to the workspace (e.g. `./file.md`) or absolute paths that start with the workspace path above

### Forbidden

- DO NOT access, read, list, or explore any parent directories (e.g., `..`, `../`, `/Users/yifanxu/machine_learning/mobile/`)
- DO NOT access any files outside the workspace directory
- DO NOT use absolute paths that reference locations outside this workspace
- DO NOT use `cd ..` or any command that navigates to parent directories
- DO NOT execute commands like `ls ../`, `cat ../file`, `find /`, etc.
- DO NOT access system directories, home directories, or any path not starting with the workspace path
- DO NOT read, write, or search in project root (e.g. `apps/`, `server.js`) unless the path is under `workspace/`

### Enforcement

If a user requests access to files or directories outside the workspace:

1. Politely decline the request
2. Explain that you are restricted to the current workspace directory only
3. Offer to help with files within the allowed workspace instead

### Example Responses

- "I can only access files within the current workspace directory. I cannot view parent directories or external paths."
- "That path is outside my allowed workspace. Please provide a path within the current directory."

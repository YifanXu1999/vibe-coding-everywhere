# Terminal PTY

Web-based terminal that connects to a local pseudo-terminal (PTY) via Node.js.

## Setup

```bash
cd /Users/yifanxu/machine_learning/mobile-coder/apps/mobile/workspace
npm install
npm start
```

## Usage

Open http://localhost:3456 in your browser. The terminal uses the workspace directory as its working directory.
